<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moda Studio | Đăng ký</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f4f1ee;
            font-family: "Helvetica Neue", Arial, sans-serif;
        }

        .auth-card {
            width: 100%;
            max-width: 520px;
            background: #fff;
            border-radius: 24px;
            padding: 45px;
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.15);
        }

        .brand-title {
            letter-spacing: 0.4em;
            font-size: 0.8rem;
            color: #b48b57;
        }
    </style>
</head>

<body>
    <div class="auth-card">
        <p class="brand-title text-uppercase mb-2">Moda Studio</p>
        <h2 class="fw-bold">Tạo tài khoản thành viên</h2>
        <p class="text-muted mb-4">Nhận thông báo ưu đãi, lịch sự kiện và bộ sưu tập giới hạn.</p>

        <% if (successMessage) { %>
            <div class="alert alert-success">
                <%= successMessage %>
            </div>
            <% } %>

                <% if (errorMessage) { %>
                    <div class="alert alert-danger">
                        <%= errorMessage %>
                    </div>
                    <% } %>

                        <form action="/user/create-user" method="POST">
                            <input type="hidden" name="source" value="signup">
                            <div class="row g-3">
                                <div class="col-12">
                                    <label for="email" class="form-label">Email</label>
                                    <input type="email" class="form-control" id="email" name="email"
                                        value="<%= formData && formData.email ? formData.email : '' %>" required>
                                </div>
                                <div class="col-md-6">
                                    <label for="password" class="form-label">Mật khẩu</label>
                                    <input type="password" class="form-control" id="password" name="password"
                                        minlength="6" required>
                                </div>
                                <div class="col-md-6">
                                    <label for="username" class="form-label">Tên hiển thị</label>
                                    <input type="text" class="form-control" id="username" name="username"
                                        value="<%= formData && formData.username ? formData.username : '' %>" required>
                                </div>
                                <div class="col-12">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" id="terms" required>
                                        <label class="form-check-label" for="terms">
                                            Tôi đồng ý với điều khoản sử dụng và chính sách bảo mật.
                                        </label>
                                    </div>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-dark w-100 py-2 mt-4">Đăng ký</button>
                        </form>

                        <p class="text-center text-muted mt-4 mb-0">
                            Đã có tài khoản?
                            <a href="/signin" class="text-decoration-none">Đăng nhập</a>
                        </p>
                        <div class="text-center mt-3">
                            <a href="/" class="text-decoration-none small text-muted">← Quay lại trang chủ</a>
                        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>